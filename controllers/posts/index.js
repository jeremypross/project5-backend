const router = require('express').Router();
const controller = require('./controller');

// all posts - router.get('/', controller.index);
// user posts - router.get('/:id', controller.show);



module.exports = router;
